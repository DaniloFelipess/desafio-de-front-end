# Server-side weather API configuration.
# These variables are intentionally not prefixed with NEXT_PUBLIC because the
# weather request runs on the server in src/app/api/weather/[city]/route.ts.

WEATHER_API_BASE_URL=https://api.open-meteo.com/v1
WEATHER_API_TIMEOUT_MS=1200
